const makeNewPlayer = (
    name, position, club, shirt, id, country
) => {
    return {
        name,
        position,
        club,
        shirt,
        id,
        country
    }
}

export default makeNewPlayer